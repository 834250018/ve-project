spring:
  cloud:
    nacos:
      config:
        server-addr: 192.168.0.114:10202
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}
  application:
    name: message
  profiles:
    active: local
