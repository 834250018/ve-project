spring:
  cloud:
    nacos:
      config:
        server-addr: 192.168.1.104:10202
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}
  application:
    name: commons
  profiles:
    active: local
